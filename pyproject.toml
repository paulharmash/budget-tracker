[project]
name = "budget-tracker"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "questionary>=2.0",
    "requests>=2.32",
    "urllib3<2"
]

[project.scripts]
budget-tracker = "budget_tracker.main:main"

[tool.setuptools.packages.find]
include = ['budget_tracker*']